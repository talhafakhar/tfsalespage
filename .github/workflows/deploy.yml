name: 🚀 Deploy to VPS (Manual Tag Deploy)

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Git tag to deploy (e.g., v1.2.3)'
        required: true
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🛠 SSH and deploy
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 22
          script: |
            echo "🚀 Deploying tag ${{ github.event.inputs.tag }} to /usr/local/lsws/tfbizsol"
            
            # Move to target directory
            cd /usr/local/lsws/tfbizsol

            echo "🔄 Fetching tag and resetting code"
            git fetch --all --tags
            git reset --hard ${{ github.event.inputs.tag }}

            echo "📦 Installing dependencies & building"
            npm install --production
            npm run build

            echo "🚀 Restarting PM2 process"
            pm2 restart tfbizsol
            pm2 save
